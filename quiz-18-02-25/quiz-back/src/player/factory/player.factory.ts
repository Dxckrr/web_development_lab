import PlayerController from "../controller/player.controller";
import PlayerModel from "../model/player.model";
import PlayerView from "../view/player.view";

export default class ContactFactory {
    // public static createPlayerView(): PlayerView {
    //     const playerModel = new PlayerModel()
    //     const playerController = new PlayerController(playerModel);
    //     return new PlayerView(playerController)
    // }
}